import { ThemeConfig } from './theme-config';

export const DEFAULT_THEME: ThemeConfig = {
  themeId: 'default-theme-id',
  mainColors: {
    primaryColor: '#0c3edf',
    accentColor: '#46a35e',
    warnColor: '#ff0700',
    headerColor: '#FFFFFF',
  },
};
